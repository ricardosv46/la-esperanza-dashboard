mutation UpdateBancos($input: BancoInput) {
  UpdateBancos(input: $input) {
    bancoId
    titulo
    estado
    numeroCuenta
    imagenPrincipal {
      url
      estado
      titulo
      id
    }
    created_at
    updated_at
  }
}
