mutation CreateBancos($input: BancoInput) {
  CreateBancos(input: $input) {
    bancoId
    titulo
    estado
    numeroCuenta
    imagenPrincipal {
      url
      estado
      titulo
      id
    }
    created_at
    updated_at
  }
}
