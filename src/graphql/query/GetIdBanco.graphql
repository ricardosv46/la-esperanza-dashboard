query GetBancoId($bancoId: Int) {
  GetBancoId(bancoId: $bancoId) {
    bancoId
    titulo
    estado
    numeroCuenta
    imagenPrincipal {
      url
      titulo
      id
      estado
    }
    created_at
    updated_at
  }
}
